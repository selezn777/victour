-- Виктор в Telegram (2026-08-30, фото списка туров): в общем списке туров
-- (/tours, полноэкранная колода с большими фото — TourPhotoSlide) на месте,
-- где раньше был Хон Там, всё ещё показывался Хон Там, а не Фанранг —
-- предыдущий свап (20260828150000_fanrang_tour.sql, коммит 7efc644) был
-- сделан только в подборке "4 тура" на главной (advantages-section.tsx,
-- захардкожена), а сам каталог /tours сортируется по sort_order из БД и
-- его не затронул.
--
-- Меняю местами sort_order: Фанранг занимает старую позицию Хон Тама (4),
-- Хон Там уходит на последнее место (6) — остаётся активным бронируемым
-- туром (в перспективе задел под адд-он, см. комментарий в
-- fanrang_tour.sql), просто больше не первый в списке.
update tours set sort_order = 4 where slug = 'fanrang-avtorskiy';
update tours set sort_order = 6 where slug = 'hon-tam';

-- У Фанранга не было hero_image_url/галереи (только текст в подборке на
-- главной) — теперь, когда тур виден в /tours со слайдом больших фото
-- (TourPhotoSlide), это выглядело бы пустым местом вместо фото. Ставлю ту
-- же временную заглушку, что уже используется в advantages-section.tsx
-- (public/images/tours/fanrang-avtorskiy.jpg) — Виктор подтверждал раньше,
-- что фото временное и будет заменено позже, это не блокирует.
update tours set hero_image_url = '/images/tours/fanrang-avtorskiy.jpg' where slug = 'fanrang-avtorskiy';
